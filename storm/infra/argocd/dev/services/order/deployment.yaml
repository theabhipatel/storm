apiVersion: apps/v1
kind: Deployment
metadata:
  name: order
  namespace: storm
  labels:
    app: order
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: order
      annotations:
        linkerd.io/inject: enabled
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3006"
    spec:
      serviceAccountName: order
      containers:
        - name: app
          # Image tag is bumped by the CD workflow on each merge to main.
          image: 000000000000.dkr.ecr.ap-south-1.amazonaws.com/storm/order:replace-me
          ports:
            - { name: http, containerPort: 3006 }
          env:
            - { name: NODE_ENV,                   value: production }
            - { name: PORT,                       value: "3006" }
            - { name: SERVICE_VERSION,            valueFrom: { fieldRef: { fieldPath: metadata.labels['app.kubernetes.io/version'] } } }
            - { name: DEPLOYMENT_ENVIRONMENT,     value: dev }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT,value: http://otel-collector.observability.svc.cluster.local:4318 }
          envFrom:
            - secretRef: { name: order-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1,    memory: 1Gi }
          readinessProbe:
            httpGet: { path: /ready,  port: 3006 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 3006 }
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: order
  namespace: storm
spec:
  selector:
    app: order
  ports:
    - { name: http, port: 3006, targetPort: 3006 }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: order
  namespace: storm
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: order-secrets
  namespace: storm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: order-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: storm/dev/order
