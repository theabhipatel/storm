apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-bff
  namespace: storm
  labels:
    app: admin-bff
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-bff
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: admin-bff
      annotations:
        linkerd.io/inject: enabled
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3100"
    spec:
      serviceAccountName: admin-bff
      containers:
        - name: app
          # Image tag is bumped by the CD workflow on each merge to main.
          image: 000000000000.dkr.ecr.ap-south-1.amazonaws.com/storm/admin-bff:replace-me
          ports:
            - { name: http, containerPort: 3100 }
          env:
            - { name: NODE_ENV,                   value: production }
            - { name: PORT,                       value: "3100" }
            - { name: SERVICE_VERSION,            valueFrom: { fieldRef: { fieldPath: metadata.labels['app.kubernetes.io/version'] } } }
            - { name: DEPLOYMENT_ENVIRONMENT,     value: dev }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT,value: http://otel-collector.observability.svc.cluster.local:4318 }
          envFrom:
            - secretRef: { name: admin-bff-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1,    memory: 1Gi }
          readinessProbe:
            httpGet: { path: /ready,  port: 3100 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 3100 }
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: admin-bff
  namespace: storm
spec:
  selector:
    app: admin-bff
  ports:
    - { name: http, port: 3100, targetPort: 3100 }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-bff
  namespace: storm
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: admin-bff-secrets
  namespace: storm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: admin-bff-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: storm/dev/admin-bff
