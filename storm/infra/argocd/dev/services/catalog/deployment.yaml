apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalog
  namespace: storm
  labels:
    app: catalog
spec:
  replicas: 2
  selector:
    matchLabels:
      app: catalog
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: catalog
      annotations:
        linkerd.io/inject: enabled
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3002"
    spec:
      serviceAccountName: catalog
      containers:
        - name: app
          # Image tag is bumped by the CD workflow on each merge to main.
          image: 000000000000.dkr.ecr.ap-south-1.amazonaws.com/storm/catalog:replace-me
          ports:
            - { name: http, containerPort: 3002 }
          env:
            - { name: NODE_ENV,                   value: production }
            - { name: PORT,                       value: "3002" }
            - { name: SERVICE_VERSION,            valueFrom: { fieldRef: { fieldPath: metadata.labels['app.kubernetes.io/version'] } } }
            - { name: DEPLOYMENT_ENVIRONMENT,     value: dev }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT,value: http://otel-collector.observability.svc.cluster.local:4318 }
          envFrom:
            - secretRef: { name: catalog-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1,    memory: 1Gi }
          readinessProbe:
            httpGet: { path: /ready,  port: 3002 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 3002 }
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: storm
spec:
  selector:
    app: catalog
  ports:
    - { name: http, port: 3002, targetPort: 3002 }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalog
  namespace: storm
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: catalog-secrets
  namespace: storm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: catalog-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: storm/dev/catalog
