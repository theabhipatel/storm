apiVersion: apps/v1
kind: Deployment
metadata:
  name: identity
  namespace: storm
  labels:
    app: identity
spec:
  replicas: 2
  selector:
    matchLabels:
      app: identity
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: identity
      annotations:
        linkerd.io/inject: enabled
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3001"
    spec:
      serviceAccountName: identity
      containers:
        - name: app
          # Image tag is bumped by the CD workflow on each merge to main.
          image: 000000000000.dkr.ecr.ap-south-1.amazonaws.com/storm/identity:replace-me
          ports:
            - { name: http, containerPort: 3001 }
          env:
            - { name: NODE_ENV,                   value: production }
            - { name: PORT,                       value: "3001" }
            - { name: SERVICE_VERSION,            valueFrom: { fieldRef: { fieldPath: metadata.labels['app.kubernetes.io/version'] } } }
            - { name: DEPLOYMENT_ENVIRONMENT,     value: dev }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT,value: http://otel-collector.observability.svc.cluster.local:4318 }
          envFrom:
            - secretRef: { name: identity-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1,    memory: 1Gi }
          readinessProbe:
            httpGet: { path: /ready,  port: 3001 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 3001 }
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: identity
  namespace: storm
spec:
  selector:
    app: identity
  ports:
    - { name: http, port: 3001, targetPort: 3001 }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: identity
  namespace: storm
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: identity-secrets
  namespace: storm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: identity-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: storm/dev/identity
