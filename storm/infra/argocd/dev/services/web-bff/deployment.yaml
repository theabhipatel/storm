apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-bff
  namespace: storm
  labels:
    app: web-bff
spec:
  replicas: 2
  selector:
    matchLabels:
      app: web-bff
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: web-bff
      annotations:
        linkerd.io/inject: enabled
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "3000"
    spec:
      serviceAccountName: web-bff
      containers:
        - name: app
          # Image tag is bumped by the CD workflow on each merge to main.
          image: 000000000000.dkr.ecr.ap-south-1.amazonaws.com/storm/web-bff:replace-me
          ports:
            - { name: http, containerPort: 3000 }
          env:
            - { name: NODE_ENV,                   value: production }
            - { name: PORT,                       value: "3000" }
            - { name: SERVICE_VERSION,            valueFrom: { fieldRef: { fieldPath: metadata.labels['app.kubernetes.io/version'] } } }
            - { name: DEPLOYMENT_ENVIRONMENT,     value: dev }
            - { name: OTEL_EXPORTER_OTLP_ENDPOINT,value: http://otel-collector.observability.svc.cluster.local:4318 }
          envFrom:
            - secretRef: { name: web-bff-secrets }
          resources:
            requests: { cpu: 100m, memory: 256Mi }
            limits:   { cpu: 1,    memory: 1Gi }
          readinessProbe:
            httpGet: { path: /ready,  port: 3000 }
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 3000 }
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: web-bff
  namespace: storm
spec:
  selector:
    app: web-bff
  ports:
    - { name: http, port: 3000, targetPort: 3000 }
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: web-bff
  namespace: storm
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: web-bff-secrets
  namespace: storm
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: web-bff-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: storm/dev/web-bff
